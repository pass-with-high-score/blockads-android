*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Signing & secrets
key.properties
*.jks
*.keystore
fastlane/play-store-key.json

# Fastlane
fastlane/report.xml
fastlane/README.md
app/release/app-release.aab
.idea
keystore.txt
play-store-key.json
.kotlin

# Prebuilt Go tunnel. Resolved from GitHub Releases as app.pwhs:tunnel, or built
# into build/tunnel/ by ./gradlew -Ptunnel.source=local. Never commit the aar:
# it is ~19MB of non-deltifiable binary per revision.
app/libs/
blockadstv/libs/

# Bundled filter lists – committed to repo for reproducible builds
# Run ./scripts/bundle_filters.sh before tagging a release to update

# Sentry Config File
sentry.properties
.gopath
.ruby-lsp
adguard.apk
adguard
.claude/
.playwright-mcp/